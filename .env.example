# Build-time configuration for the Grafana Incremental Trace Viewer plugin
# Copy this file to .env and modify the values as needed

# Set to '1' to enable child count support, '0' to disable it
# This affects whether the plugin will use childCount attributes from the backend
SUPPORTS_CHILD_COUNT=0
