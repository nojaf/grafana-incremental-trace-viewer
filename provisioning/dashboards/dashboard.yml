apiVersion: 1

providers:
  - name: 'GR Plugin Dashboards'
    orgId: 1
    folder: 'GR Plugin'
    folderUid: 'gr-plugin-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
